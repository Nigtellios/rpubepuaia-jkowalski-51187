import styles from './HeroSection.module.scss';

// Here we'll use https://splidejs.com/integration/react-splide/

export default function HeroSection() {
  return (
    <section className={ styles.hero }>
    </section>
  );
}
